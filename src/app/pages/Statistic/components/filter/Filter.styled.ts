import {Box, styled} from "@mui/material";

export const FilterStyled = styled(Box)`margin-top: 10px;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
`

export const SelectsGroup = styled(Box)`
    display: flex;
    flex-direction: row;
    gap: 12px;
`