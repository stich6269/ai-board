import {Box, styled} from "@mui/material";

export const StatisticStyled = styled(Box)`
  padding-top: 24px;
`

export const ChartRow = styled(Box)`
    margin-top: 36px;
    min-height: 400px;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
`